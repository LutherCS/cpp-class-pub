/*
 * This is the main (driver) file
 * It must include the implementation (cpp) file
 * Use it to run your application
 */
#include "exercise0.hpp"

int main(int argc, char * argv[]) {
    std::cout << adder(1, 2) << std::endl;

    return 0;
}
